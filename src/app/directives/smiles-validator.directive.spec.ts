import { SmilesValidatorDirective } from './smiles-validator.directive';

describe('SmilesValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SmilesValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
