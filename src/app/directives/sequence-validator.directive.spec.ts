import { SequenceValidatorDirective } from './sequence-validator.directive';

describe('SequenceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SequenceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
