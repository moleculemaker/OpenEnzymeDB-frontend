import { PickRecordPipe } from './pick-record.pipe';

describe('PickRecordPipe', () => {
  it('create an instance', () => {
    const pipe = new PickRecordPipe();
    expect(pipe).toBeTruthy();
  });
});
